package lt326

import (
	"testing"
)

// 测试浮点型数据是否能进行按位与操作
func TestFloat(t *testing.T) {
	//a := 16.7
	//b := 3.2
	//// (a/b) % 1 编辑器直接报错（编译期出错）
	//c := 1.0
	//// (a/b) % c 编辑器直接报错（编译期出错）
	//fmt.Println( a % c)
	// 结论： 浮点数不能按位与，同理其他位操作也不行
}
